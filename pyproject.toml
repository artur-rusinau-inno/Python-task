[project]
name = "python-task"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "asyncpg>=0.31.0",
    "dotenv>=0.9.9",
    "pytest>=9.0.3",
    "typer>=0.25.1",
    "xmltodict>=1.0.4",
]

[dependency-groups]
dev = []
